[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  BUN_VERSION = "1.2.5"

# Configurar Bun como gestor de paquetes
[[plugins]]
  package = "@netlify/plugin-bun"
